import React from 'react'
import { homeSections } from '../../../constants/systemTypes'

const Projects = () => {
  return (
    <div className="app__container"
      id={homeSections.PROJECTS}
      style={{ height: '100vh' }}
    >
      <h1>Projects</h1>
    </div>
  )
}

export default Projects